# Base URL of the FastAPI backend (no trailing slash). Inlined at build time.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Set to 1 on the replay-only deploy (no backend): "Investigate" explains itself and points at the recorded run.
# NEXT_PUBLIC_REPLAY_ONLY=1
